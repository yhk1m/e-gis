# 보로노이 다이어그램 (티센 폴리곤) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** e-GIS 벡터 분석에 포인트 레이어로부터 보로노이 셀(티센 폴리곤)을 생성하는 기능을 추가한다.

**Architecture:** 기존 도구 패턴 4겹(Tool 로직 싱글턴 + Panel 모달 싱글턴 + AppLayout 드롭다운 항목 + main.js 액션 switch)을 그대로 따른다. 전 계산을 EPSG:3857에서 수행하고 면적만 `ol/sphere`로 측지 계산한다. 순수 헬퍼는 의존성 없는 별도 모듈로 분리해 vitest로 테스트한다.

**Tech Stack:** Vanilla JS (ES modules), Vite 5, OpenLayers 9, `@turf/turf` 7.3.1 (`@turf/voronoi` 포함, 이미 설치됨), vitest (신규)

**설계 문서:** `docs/superpowers/specs/2026-07-15-voronoi-design.md`

---

## 스펙에서의 의도적 변경 1건

스펙 "테스트 대상 순수 헬퍼" 절은 헬퍼를 `VoronoiTool.js` 안에 두고 export하라고 적혀 있다.
**이 계획은 헬퍼를 별도 파일 `src/tools/voronoiHelpers.js`로 분리한다.**

이유: `VoronoiTool.js`는 `layerManager`를 import하고, `layerManager`는 `mapManager`를,
`mapManager`는 OpenLayers 전체와 `src/ui/MapScaleBar.js`를 모듈 로드 시점에 끌어온다.
헬퍼가 같은 파일에 있으면 순수 함수 테스트가 이 체인을 통째로 로드하게 되어 느리고 깨지기 쉽다.
분리하면 테스트가 import 하나로 끝나고, 파일 책임도 더 명확해진다.

## 파일 구조

| 파일 | 책임 |
|------|------|
| `src/tools/voronoiHelpers.js` (신규) | 순수 함수 3개. **import 없음.** 좌표/이름 계산만 담당 |
| `src/tools/voronoiHelpers.test.js` (신규) | 위 함수들의 단위 테스트 |
| `src/tools/VoronoiTool.js` (신규) | OL/turf/layerManager를 엮는 파이프라인. DOM 모름 |
| `src/ui/panels/VoronoiPanel.js` (신규) | 모달 UI. alert로 에러/결과 보고 |
| `src/ui/layout/AppLayout.js` (수정) | 벡터 분석 드롭다운 항목 1줄 |
| `src/main.js` (수정) | import 1줄 + switch case |
| `src/styles/main.css` (수정) | 기존 `.heatmap-modal` 규칙에 `.voronoi-modal` 셀렉터 추가 |
| `package.json` (수정) | vitest devDependency + test 스크립트 |
| `vitest.config.js` (신규) | 테스트 범위를 `src/`로 한정 (아래 참조) |

## 실행 중 발견 (Task 1)

`eStoryMap/`은 자체 테스트 스위트를 가진 **별도 하위 프로젝트**다. 루트에 vitest를 깔면
설정 없이는 `eStoryMap/src/**/*.test.js`까지 자동 발견해서 22개 테스트가 실패한 채로 돈다.
그러면 Task 2~4의 "npm test → PASS" 게이트가 영구히 빨간불이라 무의미해진다.

따라서 Task 1에 `vitest.config.js` 생성을 추가해 `include: ['src/**/*.test.js']`로 범위를 좁힌다.
`vite.config.js`는 건드리지 않는다.

## 실행 중 발견 (Task 5) — `npm run build`가 이 PC에서 죽는다

**보로노이 작업과 무관한 기존 환경 문제다.** `npm run build`(= `vite build`)가
`✓ 2336 modules transformed.` 직후 rollup 네이티브 바이너리에서 크래시한다.
종료 코드 `-1073740791` (`0xC0000409`, STATUS_STACK_BUFFER_OVERRUN). JS 에러도 스택도 없다.

확인된 사실:
- `VoronoiTool.js`를 트리에서 빼도 동일하게 크래시 → 우리 코드 탓이 아니다.
- rollup 4.54.0은 vitest 설치 **이전**(`783eb2d`)부터 lock에 있었다 → 우리가 올린 게 아니다.
- PowerShell/Git Bash 양쪽 모두 동일. `native.js:50`이 `isMingw32()`로 갈리지만
  Git Bash도 mingw로 잡히지 않아 결국 `win32-x64-msvc`를 집는다.
- `@rollup/rollup-win32-x64-gnu`와 `-msvc`가 **둘 다** 설치돼 있다 (비정상).
- Node v24.12.0. rollup의 `engines`는 `>=18`이라 명목상 지원.

**영향 없음이 확인된 것:**
- `npm run dev`는 정상 (333ms 기동, `/` 200, `src/tools/VoronoiTool.js` 200).
  Vite 개발 서버는 rollup 번들러를 타지 않는다 → **Task 10 수동 검증에 지장 없다.**
- 배포는 Vercel 클라우드(Linux) 빌드라 이 Windows 네이티브 크래시와 무관할 가능성이 높다.

**따라서 Task 5·6·7·9의 `npm run build` 게이트를 아래로 대체한다:**

```bash
npx esbuild src/tools/VoronoiTool.js --bundle --format=esm --platform=browser --outfile=/dev/null
```

import 해석만 검증하는 용도다. 성공하면 경로가 맞는 것이다.
(`src/main.js`를 대상으로 하면 앱 전체 import 그래프를 검증할 수 있다.)

빌드 크래시 자체는 **별건으로 조사할 사안**이며 이 계획의 범위 밖이다.

---

### Task 1: vitest 도입

**Files:**
- Modify: `package.json`
- Create: `vitest.config.js`

- [ ] **Step 1: vitest 설치**

```bash
cd "C:/Users/김용현/Desktop/vibecoding/eGIS"
npm install -D vitest
```

- [ ] **Step 2: test 스크립트 추가**

`package.json`의 `scripts`에 `test`를 추가한다. 변경 후 `scripts` 전체 모습:

```json
  "scripts": {
    "catalog": "node scripts/generate-catalog.cjs",
    "dev": "vite",
    "build": "npm run catalog && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "vitest run"
  },
```

- [ ] **Step 3: 테스트 범위를 `src/`로 한정**

설정 없이 두면 vitest가 하위 프로젝트 `eStoryMap/`의 테스트까지 발견해 실행한다.
그쪽은 자체 스위트와 setup을 가진 별개 프로젝트라 루트 `npm test`가 끌어오면 안 된다.

`vitest.config.js` 생성 (`vite.config.js`는 건드리지 않는다):

```js
import { defineConfig } from 'vitest/config';

export default defineConfig({
  // eStoryMap/은 자체 테스트 스위트를 가진 별도 하위 프로젝트다.
  // 루트 npm test는 e-GIS 본체(src/)만 대상으로 한다.
  test: {
    include: ['src/**/*.test.js']
  }
});
```

`defineConfig`를 `vite`가 아니라 **`vitest/config`**에서 import해야 한다.

- [ ] **Step 4: 러너가 도는지 확인**

Run: `npm test`

Expected: "No test files found" 로 종료하고 **eStoryMap 테스트가 목록에 없다.**
`src/`에는 아직 테스트가 없는 게 맞다 (Task 2가 첫 테스트를 추가한다).
러너 자체가 실행되면 성공이다. `vitest: command not found` 가 나오면 Step 1이 실패한 것이다.
eStoryMap 테스트가 여전히 돌면 Step 3의 설정이 안 먹은 것이다.

- [ ] **Step 5: 커밋**

```bash
git add package.json package-lock.json vitest.config.js
git commit -m "chore: vitest 도입 (순수 헬퍼 단위 테스트용)"
```

---

### Task 2: `dedupeSeeds` — 중복 좌표 제거

**중요:** 이 함수는 선택 사항이 아니다. `turf.voronoi`는 내부적으로 `d3-voronoi`를 쓰는데,
겹친 좌표에 `null` 셀을 돌려주고 turf가 그걸 그대로 `coords.slice()` 해서 `TypeError`로 죽는다.
(`node_modules/@turf/voronoi/dist/esm/index.js:7`)

**시드 자료형:** `{ coord: [x, y], properties: {...} }` — `coord`는 EPSG:3857 미터.

**Files:**
- Create: `src/tools/voronoiHelpers.js`
- Test: `src/tools/voronoiHelpers.test.js`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/tools/voronoiHelpers.test.js`:

```js
import { describe, it, expect } from 'vitest';
import { dedupeSeeds } from './voronoiHelpers.js';

const seed = (x, y, props = {}) => ({ coord: [x, y], properties: props });

describe('dedupeSeeds', () => {
  it('중복이 없으면 그대로 돌려준다', () => {
    const input = [seed(0, 0), seed(100, 100), seed(200, 50)];
    const result = dedupeSeeds(input);
    expect(result.seeds).toHaveLength(3);
    expect(result.duplicates).toBe(0);
  });

  it('정확히 겹친 좌표를 하나로 줄인다', () => {
    const input = [seed(0, 0), seed(100, 100), seed(0, 0)];
    const result = dedupeSeeds(input);
    expect(result.seeds).toHaveLength(2);
    expect(result.duplicates).toBe(1);
  });

  it('중복 중 첫 번째의 속성을 남긴다', () => {
    const input = [seed(0, 0, { name: '첫째' }), seed(0, 0, { name: '둘째' })];
    const result = dedupeSeeds(input);
    expect(result.seeds).toHaveLength(1);
    expect(result.seeds[0].properties.name).toBe('첫째');
  });

  it('전부 같은 좌표면 하나만 남는다', () => {
    const input = [seed(5, 5), seed(5, 5), seed(5, 5)];
    const result = dedupeSeeds(input);
    expect(result.seeds).toHaveLength(1);
    expect(result.duplicates).toBe(2);
  });

  it('1mm 미만 차이는 같은 점으로 본다', () => {
    const input = [seed(0, 0), seed(0.0002, 0.0002)];
    const result = dedupeSeeds(input);
    expect(result.seeds).toHaveLength(1);
    expect(result.duplicates).toBe(1);
  });

  it('1m 차이는 다른 점으로 본다', () => {
    const input = [seed(0, 0), seed(1, 0)];
    const result = dedupeSeeds(input);
    expect(result.seeds).toHaveLength(2);
    expect(result.duplicates).toBe(0);
  });

  it('빈 배열을 처리한다', () => {
    const result = dedupeSeeds([]);
    expect(result.seeds).toEqual([]);
    expect(result.duplicates).toBe(0);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`

Expected: FAIL — `Failed to resolve import "./voronoiHelpers.js"` (파일이 아직 없다)

- [ ] **Step 3: 최소 구현**

`src/tools/voronoiHelpers.js` 생성:

```js
/**
 * voronoiHelpers - 보로노이 도구의 순수 헬퍼
 *
 * 이 파일은 아무것도 import하지 않는다. VoronoiTool.js는 layerManager를 통해
 * OpenLayers 전체를 끌어오므로, 순수 계산을 여기로 분리해 단위 테스트를 가볍게 유지한다.
 * 좌표는 모두 EPSG:3857 미터 단위다.
 */

/** 중복 판정 격자 크기(m). 1mm. */
const DEDUPE_PRECISION = 0.001;

/**
 * 겹친 시드를 제거한다.
 *
 * turf.voronoi가 중복 좌표에서 TypeError로 죽기 때문에 반드시 선행되어야 한다.
 *
 * @param {Array<{coord: number[], properties: Object}>} seeds
 * @returns {{seeds: Array, duplicates: number}} duplicates는 제거된 개수
 */
export function dedupeSeeds(seeds) {
  const seen = new Set();
  const kept = [];
  let duplicates = 0;

  for (const seed of seeds) {
    const key =
      Math.round(seed.coord[0] / DEDUPE_PRECISION) + ',' +
      Math.round(seed.coord[1] / DEDUPE_PRECISION);

    if (seen.has(key)) {
      duplicates++;
      continue;
    }
    seen.add(key);
    kept.push(seed);
  }

  return { seeds: kept, duplicates };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`

Expected: PASS — `dedupeSeeds` 7개 테스트 전부 통과

- [ ] **Step 5: 커밋**

```bash
git add src/tools/voronoiHelpers.js src/tools/voronoiHelpers.test.js
git commit -m "feat: dedupeSeeds - 보로노이 중복 좌표 제거

turf.voronoi가 겹친 좌표에서 TypeError로 죽으므로 필수 선행 단계."
```

---

### Task 3: `computeBBox` — 클립 사각형 산정

**Files:**
- Modify: `src/tools/voronoiHelpers.js`
- Test: `src/tools/voronoiHelpers.test.js`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/tools/voronoiHelpers.test.js`의 import 줄을 바꾸고 describe 블록을 파일 끝에 추가한다.

import 줄을 이렇게 교체:

```js
import { dedupeSeeds, computeBBox } from './voronoiHelpers.js';
```

파일 끝에 추가:

```js
describe('computeBBox', () => {
  it('경계 없이 폭·높이의 10%를 각 변에 더한다', () => {
    // extent [0,0,100,100] → 폭 100, 높이 100 → 여유 10씩
    expect(computeBBox([0, 0, 100, 100], null)).toEqual([-10, -10, 110, 110]);
  });

  it('경계 extent와 합집합을 취한 뒤 여유를 준다', () => {
    // seed [0,0,100,100] ∪ boundary [-50,-50,50,50] = [-50,-50,100,100]
    // 폭 150, 높이 150 → 여유 15씩
    expect(computeBBox([0, 0, 100, 100], [-50, -50, 50, 50]))
      .toEqual([-65, -65, 115, 115]);
  });

  it('폭이 0이면(수직 일직선) 절대 여유 1000m를 쓴다', () => {
    expect(computeBBox([10, 0, 10, 100], null)).toEqual([-990, -10, 1010, 110]);
  });

  it('높이가 0이면(수평 일직선) 절대 여유 1000m를 쓴다', () => {
    expect(computeBBox([0, 50, 100, 50], null)).toEqual([-10, -950, 110, 1050]);
  });

  it('한 점이면 양축 모두 절대 여유를 쓴다', () => {
    expect(computeBBox([5, 5, 5, 5], null)).toEqual([-995, -995, 1005, 1005]);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`

Expected: FAIL — `computeBBox is not a function`

- [ ] **Step 3: 최소 구현**

`src/tools/voronoiHelpers.js`의 `DEDUPE_PRECISION` 상수 아래에 추가:

```js
/** bbox 여유 비율 (기준 extent 폭·높이 대비) */
const BBOX_PADDING_RATIO = 0.1;

/** extent 폭 또는 높이가 0일 때 쓸 절대 여유(m) */
const BBOX_MIN_PADDING = 1000;
```

파일 끝에 추가:

```js
/**
 * 보로노이 클립 사각형을 만든다.
 *
 * 경계 레이어가 있으면 시드 extent와 합집합을 취한다. 경계 밖 시드도 경계 안쪽으로
 * 셀을 밀어넣기 때문에 시드 extent만 쓰면 안 된다.
 *
 * @param {number[]} seedExtent - [minX, minY, maxX, maxY] (EPSG:3857)
 * @param {number[]|null} boundaryExtent - 경계 레이어 extent, 없으면 null
 * @returns {number[]} [minX, minY, maxX, maxY]
 */
export function computeBBox(seedExtent, boundaryExtent = null) {
  let [minX, minY, maxX, maxY] = seedExtent;

  if (boundaryExtent) {
    minX = Math.min(minX, boundaryExtent[0]);
    minY = Math.min(minY, boundaryExtent[1]);
    maxX = Math.max(maxX, boundaryExtent[2]);
    maxY = Math.max(maxY, boundaryExtent[3]);
  }

  const width = maxX - minX;
  const height = maxY - minY;

  // 시드가 한 점이거나 일직선이면 비율 여유가 0이 되어 bbox가 축퇴한다.
  const padX = width > 0 ? width * BBOX_PADDING_RATIO : BBOX_MIN_PADDING;
  const padY = height > 0 ? height * BBOX_PADDING_RATIO : BBOX_MIN_PADDING;

  return [minX - padX, minY - padY, maxX + padX, maxY + padY];
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`

Expected: PASS — `dedupeSeeds` 7개 + `computeBBox` 5개 = 12개 통과

- [ ] **Step 5: 커밋**

```bash
git add src/tools/voronoiHelpers.js src/tools/voronoiHelpers.test.js
git commit -m "feat: computeBBox - 보로노이 클립 사각형 산정

경계 extent와 합집합을 취하고, 축퇴 extent에 절대 여유를 적용."
```

---

### Task 4: `resolveFieldName` — 필드명 충돌 회피

**Files:**
- Modify: `src/tools/voronoiHelpers.js`
- Test: `src/tools/voronoiHelpers.test.js`

- [ ] **Step 1: 실패하는 테스트 작성**

import 줄을 이렇게 교체:

```js
import { dedupeSeeds, computeBBox, resolveFieldName } from './voronoiHelpers.js';
```

파일 끝에 추가:

```js
describe('resolveFieldName', () => {
  it('충돌이 없으면 그대로 쓴다', () => {
    expect(resolveFieldName('area_km2', ['name', 'pop'])).toBe('area_km2');
  });

  it('충돌하면 _1을 붙인다', () => {
    expect(resolveFieldName('area_km2', ['name', 'area_km2'])).toBe('area_km2_1');
  });

  it('연속 충돌하면 빈 번호를 찾는다', () => {
    expect(resolveFieldName('area_km2', ['area_km2', 'area_km2_1', 'area_km2_2']))
      .toBe('area_km2_3');
  });

  it('기존 키가 없으면 그대로 쓴다', () => {
    expect(resolveFieldName('area_km2', [])).toBe('area_km2');
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`

Expected: FAIL — `resolveFieldName is not a function`

- [ ] **Step 3: 최소 구현**

`src/tools/voronoiHelpers.js` 끝에 추가:

```js
/**
 * 기존 속성과 충돌하지 않는 필드명을 만든다.
 *
 * 원본 포인트에 이미 area_km2가 있을 수 있으므로 조용히 덮어쓰지 않는다.
 *
 * @param {string} baseName
 * @param {string[]} existingKeys
 * @returns {string}
 */
export function resolveFieldName(baseName, existingKeys) {
  const taken = new Set(existingKeys);
  if (!taken.has(baseName)) return baseName;

  let i = 1;
  while (taken.has(`${baseName}_${i}`)) i++;
  return `${baseName}_${i}`;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`

Expected: PASS — 총 16개 통과

- [ ] **Step 5: 커밋**

```bash
git add src/tools/voronoiHelpers.js src/tools/voronoiHelpers.test.js
git commit -m "feat: resolveFieldName - area_km2 필드명 충돌 회피"
```

---

### Task 5: `VoronoiTool` — 사각형 모드

경계 클립 없이 셀을 만들고 레이어를 생성하는 데까지. 클립은 Task 6.

**Files:**
- Create: `src/tools/VoronoiTool.js`

- [ ] **Step 1: VoronoiTool 작성**

`src/tools/VoronoiTool.js`:

```js
/**
 * VoronoiTool - 보로노이 다이어그램(티센 폴리곤) 생성 도구
 *
 * 좌표계: 전 과정을 EPSG:3857(지도 좌표계)에서 계산한다. 프로젝트 관례("turf는 4326")와
 * 다른데, turf.voronoi는 측지 연산이 아니라 평면 연산이라 좌표계를 고를 수 있기 때문이다.
 * 4326(경위도)으로 계산하면 위도 37°에서 경도 1°≈88.8km / 위도 1°≈111km라
 * 약 25%의 이방성 왜곡이 생겨 셀이 찌그러진다.
 * 근거: docs/superpowers/specs/2026-07-15-voronoi-design.md
 *
 * 면적만 예외로 ol/sphere getArea로 측지 계산한다 (turf.area는 4326을 가정하므로 못 씀).
 */

import * as turf from '@turf/turf';
import GeoJSON from 'ol/format/GeoJSON';
import { getArea } from 'ol/sphere';
import { layerManager } from '../core/LayerManager.js';
import { dedupeSeeds, computeBBox, resolveFieldName } from './voronoiHelpers.js';

const AREA_FIELD = 'area_km2';
const DEFAULT_COLOR = '#3388ff';

class VoronoiTool {
  constructor() {
    this.geoJSONFormat = new GeoJSON();
  }

  /**
   * 포인트 레이어 목록
   */
  getPointLayers() {
    return layerManager.getAllLayers().filter(layer =>
      layer.geometryType === 'Point' || layer.geometryType === 'MultiPoint'
    );
  }

  /**
   * 폴리곤 레이어 목록 (경계 클립 후보)
   */
  getPolygonLayers() {
    return layerManager.getAllLayers().filter(layer =>
      layer.geometryType === 'Polygon' || layer.geometryType === 'MultiPolygon'
    );
  }

  /**
   * 보로노이 다이어그램 생성
   *
   * @param {string} layerId - 포인트 레이어 ID
   * @param {Object} options
   * @param {string|null} options.boundaryLayerId - 경계 레이어 ID, null이면 사각형 모드
   * @param {string} options.color - 결과 레이어 색상
   * @returns {{layerId: string, layerName: string, cellCount: number,
   *            skipped: {duplicates: number, outsideBoundary: number}}}
   */
  createVoronoi(layerId, options = {}) {
    const layerInfo = layerManager.getLayer(layerId);
    if (!layerInfo) {
      throw new Error('레이어를 찾을 수 없습니다.');
    }

    // 1~2. 시드 추출 + 중복 제거
    const { seeds, duplicates } = dedupeSeeds(this.extractSeeds(layerInfo));

    // 3. 최소 개수
    if (seeds.length < 2) {
      throw new Error(
        `보로노이를 만들려면 서로 다른 위치의 점이 2개 이상 필요합니다 (현재 ${seeds.length}개).`
      );
    }

    // 4. bbox (사각형 모드이므로 경계 extent 없음)
    const bbox = computeBBox(this.extentOf(seeds), null);

    // 5. 보로노이 생성. 3857 좌표를 그대로 먹인다.
    const cells = this.buildCells(seeds, bbox);

    // 6. 면적 필드 + OL 피처
    const olFeatures = this.toOLFeaturesWithArea(cells);

    // 7. 레이어 생성
    const layerName = `${layerInfo.name}_보로노이`;
    const newLayerId = layerManager.addLayer({
      name: layerName,
      features: olFeatures,
      color: options.color || DEFAULT_COLOR
    });

    return {
      layerId: newLayerId,
      layerName,
      cellCount: olFeatures.length,
      skipped: { duplicates, outsideBoundary: 0 }
    };
  }

  /**
   * OL 피처에서 시드를 뽑는다. 좌표는 EPSG:3857 원좌표 그대로.
   * MultiPoint는 각 점을 개별 시드로 펼치되 부모 속성을 공유한다.
   */
  extractSeeds(layerInfo) {
    const features = layerInfo.olLayer.getSource().getFeatures();
    const seeds = [];

    for (const feature of features) {
      const geom = feature.getGeometry();
      if (!geom) continue;

      const properties = { ...feature.getProperties() };
      delete properties.geometry;

      const type = geom.getType();
      if (type === 'Point') {
        seeds.push({ coord: geom.getCoordinates(), properties });
      } else if (type === 'MultiPoint') {
        for (const coord of geom.getCoordinates()) {
          seeds.push({ coord, properties: { ...properties } });
        }
      }
    }

    return seeds;
  }

  /**
   * 시드들의 extent
   */
  extentOf(seeds) {
    let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
    for (const { coord } of seeds) {
      if (coord[0] < minX) minX = coord[0];
      if (coord[1] < minY) minY = coord[1];
      if (coord[0] > maxX) maxX = coord[0];
      if (coord[1] > maxY) maxY = coord[1];
    }
    return [minX, minY, maxX, maxY];
  }

  /**
   * turf.voronoi 호출. 속성 복사는 turf가 알아서 한다(cloneProperties).
   */
  buildCells(seeds, bbox) {
    const points = turf.featureCollection(
      seeds.map(s => turf.point(s.coord, s.properties))
    );

    try {
      // filter(Boolean)은 방어용 두 번째 겹. 중복 제거가 1차 방어다.
      return turf.voronoi(points, { bbox }).features.filter(Boolean);
    } catch (error) {
      throw new Error('보로노이 생성에 실패했습니다: ' + error.message);
    }
  }

  /**
   * GeoJSON 셀 → OL 피처. 면적 필드를 붙인다.
   * readFeatures를 옵션 없이 호출하므로 좌표 변환이 없다(3857 유지).
   */
  toOLFeaturesWithArea(cells) {
    const olFeatures = this.geoJSONFormat.readFeatures(turf.featureCollection(cells));

    // 셀마다 속성 키가 다를 수 있으므로 전체 합집합을 봐야 한다.
    // TableJoinTool은 조인 키가 맞은 피처에만 필드를 붙이므로(TableJoinTool.js:237),
    // 부분 조인 후에는 일부 포인트에만 area_km2가 있다. 첫 셀만 보면 충돌을 놓쳐
    // 사용자의 원본 area_km2를 계산값으로 덮어쓴다.
    const existingKeys = [...new Set(
      cells.flatMap(c => Object.keys(c.properties || {}))
    )];
    const areaField = resolveFieldName(AREA_FIELD, existingKeys);

    for (const feature of olFeatures) {
      const areaM2 = getArea(feature.getGeometry(), { projection: 'EPSG:3857' });
      feature.set(areaField, Number((areaM2 / 1e6).toFixed(3)));
    }

    return olFeatures;
  }
}

export const voronoiTool = new VoronoiTool();
```

- [ ] **Step 2: 기존 테스트가 여전히 통과하는지 확인**

Run: `npm test`

Expected: PASS — 16개 그대로 통과 (`VoronoiTool.js`는 테스트가 import하지 않으므로 영향 없음)

- [ ] **Step 3: 빌드가 깨지지 않는지 확인**

Run: `npm run build`

Expected: 성공. `@turf/voronoi`가 번들에 포함된다.
`Could not resolve` 에러가 나면 import 경로를 확인한다.

- [ ] **Step 4: 커밋**

```bash
git add src/tools/VoronoiTool.js
git commit -m "feat: VoronoiTool - 보로노이 셀 생성 (사각형 모드)

EPSG:3857에서 평면 계산하고 면적만 ol/sphere로 측지 계산한다."
```

---

### Task 6: `VoronoiTool` — 경계 레이어 클립

**Files:**
- Modify: `src/tools/VoronoiTool.js`

- [ ] **Step 1: `buildBoundary` 메서드 추가**

`src/tools/VoronoiTool.js`의 `buildCells` 메서드 **앞에** 추가:

```js
  /**
   * 경계 레이어의 폴리곤들을 하나로 합친다.
   *
   * union이 실패하면 조용히 폴백하지 않고 에러를 던진다. 사용자가 경계를 지정했는데
   * 사각형 결과가 나오면 그걸 맞다고 믿게 되기 때문이다.
   */
  buildBoundary(boundaryLayerId) {
    const layerInfo = layerManager.getLayer(boundaryLayerId);
    if (!layerInfo) {
      throw new Error('경계 레이어를 찾을 수 없습니다.');
    }

    // 옵션 없이 write → 3857 원좌표 유지
    const geoFeatures = layerInfo.olLayer.getSource().getFeatures()
      .map(f => this.geoJSONFormat.writeFeatureObject(f))
      .filter(f => f.geometry &&
        (f.geometry.type === 'Polygon' || f.geometry.type === 'MultiPolygon'));

    if (geoFeatures.length === 0) {
      throw new Error('경계 레이어에 폴리곤 피처가 없습니다.');
    }
    if (geoFeatures.length === 1) {
      return geoFeatures[0];
    }

    try {
      const merged = turf.union(turf.featureCollection(geoFeatures));
      if (!merged) throw new Error('union 결과가 비어 있습니다.');
      return merged;
    } catch (error) {
      console.warn('경계 union 실패:', error);
      throw new Error(
        '경계 레이어의 도형을 합치는 데 실패했습니다. ' +
        '다른 경계 레이어를 쓰거나 사각형 모드로 시도해 주세요.'
      );
    }
  }

  /**
   * 셀들을 경계로 자른다. 경계 밖 셀은 버리고 개수를 센다.
   */
  clipCells(cells, boundaryFeature) {
    const kept = [];
    let outsideBoundary = 0;

    for (const cell of cells) {
      let piece = null;
      try {
        piece = turf.intersect(turf.featureCollection([cell, boundaryFeature]));
      } catch (error) {
        console.warn('셀 클립 실패:', error);
      }

      if (piece) {
        // turf.intersect는 속성을 넘겨주지 않으므로 원본 셀 속성을 다시 붙인다.
        piece.properties = { ...cell.properties };
        kept.push(piece);
      } else {
        outsideBoundary++;
      }
    }

    return { cells: kept, outsideBoundary };
  }
```

- [ ] **Step 2: `createVoronoi`에서 클립 경로 연결**

`createVoronoi` 안에서 **`// 4. bbox` 주석부터 메서드의 닫는 중괄호까지**를 아래 블록으로
통째로 교체한다. `// 1~2.` ~ `// 3.` 단계(시드 추출·중복 제거·최소 개수 검증)는 손대지 않는다.
아래 블록은 `return` 문과 메서드 닫는 중괄호를 포함한다.

```js
    // 4. 경계 준비 + bbox
    //    경계 밖 시드도 경계 안쪽으로 셀을 밀어넣으므로 extent는 합집합이어야 한다.
    const boundaryFeature = options.boundaryLayerId
      ? this.buildBoundary(options.boundaryLayerId)
      : null;
    const bbox = computeBBox(
      this.extentOf(seeds),
      boundaryFeature ? turf.bbox(boundaryFeature) : null
    );

    // 5. 보로노이 생성. 3857 좌표를 그대로 먹인다.
    let cells = this.buildCells(seeds, bbox);

    // 6. 경계 클립
    let outsideBoundary = 0;
    if (boundaryFeature) {
      const clipResult = this.clipCells(cells, boundaryFeature);
      cells = clipResult.cells;
      outsideBoundary = clipResult.outsideBoundary;
    }

    if (cells.length === 0) {
      throw new Error(
        '경계 레이어와 겹치는 셀이 없습니다. 포인트와 경계가 같은 지역인지 확인해 주세요.'
      );
    }

    // 7. 면적 필드 + OL 피처 (면적은 반드시 클립 후에 계산)
    const olFeatures = this.toOLFeaturesWithArea(cells);

    // 8. 레이어 생성
    const layerName = `${layerInfo.name}_보로노이`;
    const newLayerId = layerManager.addLayer({
      name: layerName,
      features: olFeatures,
      color: options.color || DEFAULT_COLOR
    });

    return {
      layerId: newLayerId,
      layerName,
      cellCount: olFeatures.length,
      skipped: { duplicates, outsideBoundary }
    };
  }
```

- [ ] **Step 3: 빌드 확인**

Run: `npm run build`

Expected: 성공

- [ ] **Step 4: 커밋**

```bash
git add src/tools/VoronoiTool.js
git commit -m "feat: VoronoiTool 경계 레이어 클립

union 실패 시 조용한 폴백 대신 명시적 에러를 던진다."
```

---

### Task 7: `VoronoiPanel` — 모달 UI

**Files:**
- Create: `src/ui/panels/VoronoiPanel.js`

- [ ] **Step 1: VoronoiPanel 작성**

`src/ui/panels/VoronoiPanel.js`:

```js
/**
 * VoronoiPanel - 보로노이 다이어그램(티센 폴리곤) 설정 패널
 *
 * 투명도 슬라이더가 없는 것은 의도적이다. layerManager.addLayer는 opacity를 받지 않아
 * 넣어도 동작하지 않는 죽은 컨트롤이 된다. 투명도는 레이어 패널의 스타일 편집이 담당한다.
 */

import { voronoiTool } from '../../tools/VoronoiTool.js';
import { layerManager } from '../../core/LayerManager.js';

class VoronoiPanel {
  constructor() {
    this.modal = null;
  }

  show() {
    const pointLayers = voronoiTool.getPointLayers();
    if (pointLayers.length === 0) {
      alert('보로노이 다이어그램을 만들려면 포인트 레이어가 필요합니다.');
      return;
    }
    this.render(pointLayers, voronoiTool.getPolygonLayers());
  }

  render(pointLayers, polygonLayers) {
    this.close();

    this.modal = document.createElement('div');
    this.modal.className = 'modal-overlay voronoi-modal active';
    this.modal.innerHTML = this.getModalHTML(pointLayers, polygonLayers);
    document.body.appendChild(this.modal);

    // 현재 선택된 레이어가 포인트면 기본값으로 고른다.
    const selectedId = layerManager.getSelectedLayerId();
    if (selectedId && pointLayers.some(l => l.id === selectedId)) {
      document.getElementById('voronoi-layer').value = selectedId;
    }

    this.bindEvents();
  }

  getModalHTML(pointLayers, polygonLayers) {
    const layerOptions = pointLayers.map(l =>
      '<option value="' + l.id + '">' + l.name + ' (' + l.featureCount + ')</option>'
    ).join('');

    const boundaryOptions =
      '<option value="">사각형 (경계 없음)</option>' +
      polygonLayers.map(l =>
        '<option value="' + l.id + '">' + l.name + '</option>'
      ).join('');

    return '<div class="modal-content voronoi-content">' +
      '<div class="modal-header">' +
        '<h3>보로노이 다이어그램 (티센 폴리곤)</h3>' +
        '<button class="modal-close" id="voronoi-close">&times;</button>' +
      '</div>' +
      '<div class="modal-body">' +
        '<div class="form-group">' +
          '<label for="voronoi-layer">포인트 레이어</label>' +
          '<select id="voronoi-layer">' + layerOptions + '</select>' +
        '</div>' +
        '<div class="form-group">' +
          '<label for="voronoi-boundary">경계 (클립) 레이어</label>' +
          '<select id="voronoi-boundary">' + boundaryOptions + '</select>' +
        '</div>' +
        '<div class="form-group">' +
          '<label for="voronoi-color">색상</label>' +
          '<input type="color" id="voronoi-color" value="#3388ff">' +
        '</div>' +
      '</div>' +
      '<div class="modal-footer">' +
        '<button class="btn btn-secondary" id="voronoi-cancel">취소</button>' +
        '<button class="btn btn-primary" id="voronoi-apply">생성</button>' +
      '</div>' +
    '</div>';
  }

  bindEvents() {
    document.getElementById('voronoi-close')
      .addEventListener('click', () => this.close());
    document.getElementById('voronoi-cancel')
      .addEventListener('click', () => this.close());
    document.getElementById('voronoi-apply')
      .addEventListener('click', () => this.apply());
  }

  apply() {
    const layerId = document.getElementById('voronoi-layer').value;
    const boundaryLayerId = document.getElementById('voronoi-boundary').value || null;
    const color = document.getElementById('voronoi-color').value;

    try {
      const result = voronoiTool.createVoronoi(layerId, { boundaryLayerId, color });

      // 걸러낸 게 있으면 반드시 알린다. 조용히 사라지면 데이터가 틀렸다고 오해한다.
      const s = result.skipped;
      const notes = [];
      if (s.duplicates > 0) notes.push(`중복 좌표 ${s.duplicates}개 제외`);
      if (s.nonPoint > 0) notes.push(`포인트가 아닌 피처 ${s.nonPoint}개 제외`);
      if (s.outsideBoundary > 0) notes.push(`경계 밖 셀 ${s.outsideBoundary}개 제외`);
      if (s.clipFailed > 0) notes.push(`도형 오류로 자르지 못한 셀 ${s.clipFailed}개 제외`);
      if (s.slivers > 0) notes.push(`면적이 거의 0인 조각 ${s.slivers}개 제외`);

      let message = '보로노이 다이어그램이 생성되었습니다!\n' +
        `레이어: ${result.layerName}\n` +
        `셀 개수: ${result.cellCount}`;
      if (notes.length > 0) {
        message += '\n\n' + notes.join('\n');
      }

      alert(message);
      this.close();
    } catch (error) {
      alert('보로노이 생성 실패: ' + error.message);
    }
  }

  close() {
    if (this.modal) {
      this.modal.remove();
      this.modal = null;
    }
  }
}

export const voronoiPanel = new VoronoiPanel();
```

- [ ] **Step 2: 빌드 확인**

Run: `npm run build`

Expected: 성공

- [ ] **Step 3: 커밋**

```bash
git add src/ui/panels/VoronoiPanel.js
git commit -m "feat: VoronoiPanel - 보로노이 설정 모달"
```

---

### Task 8: CSS

기존 `.heatmap-modal` 규칙에 `.voronoi-modal` 셀렉터를 추가한다.
제네릭 모달 CSS가 `.modal`을 기준으로 쓰는데 이 패널들은 `.modal-content`를 쓰기 때문에
별도 규칙이 필요하다. 규칙을 복제하는 대신 셀렉터를 그룹으로 묶는다.

**Files:**
- Modify: `src/styles/main.css:1993-2029`

- [ ] **Step 1: 셀렉터 그룹에 `.voronoi-modal` 추가**

**주의:** `.heatmap-modal .form-group select` 라는 문자열은 2017행과 2022행 두 곳에 나온다.
교체할 때 여는 중괄호(` {`)와 쉼표(`,`) 중 어느 쪽으로 끝나는지까지 포함해 대상을 특정해야
엉뚱한 규칙을 고치지 않는다.

`src/styles/main.css`에서 아래 4개 셀렉터를 각각 교체한다.

`.heatmap-modal .modal-content {` (1993행) →
```css
.heatmap-modal .modal-content,
.voronoi-modal .modal-content {
```

`.heatmap-modal .modal-body {` (2000행) →
```css
.heatmap-modal .modal-body,
.voronoi-modal .modal-body {
```

`.heatmap-modal .form-group {` (2004행) →
```css
.heatmap-modal .form-group,
.voronoi-modal .form-group {
```

`.heatmap-modal .form-group label {` (2009행) →
```css
.heatmap-modal .form-group label,
.voronoi-modal .form-group label {
```

- [ ] **Step 2: select/input 규칙에 추가**

`.heatmap-modal .form-group select,` / `.heatmap-modal .form-group input[type="range"] {` (2017-2018행) →
```css
.heatmap-modal .form-group select,
.heatmap-modal .form-group input[type="range"],
.voronoi-modal .form-group select {
```

`.heatmap-modal .form-group select {` (2022행) →
```css
.heatmap-modal .form-group select,
.voronoi-modal .form-group select {
```

- [ ] **Step 3: 커밋**

```bash
git add src/styles/main.css
git commit -m "style: 보로노이 모달 스타일 (히트맵 규칙에 셀렉터 그룹 추가)"
```

---

### Task 9: 메뉴 배선

**Files:**
- Modify: `src/ui/layout/AppLayout.js:99`
- Modify: `src/main.js:26`, `src/main.js:739-747`

- [ ] **Step 1: 드롭다운 항목 추가**

`src/ui/layout/AppLayout.js`의 99행을 찾는다:

```html
                <div class="dropdown-item" data-action="analysis-buffer">버퍼 분석</div>
```

바로 아래에 한 줄 추가한다 (버퍼 분석과 공간 연산 사이):

```html
                <div class="dropdown-item" data-action="analysis-voronoi">보로노이 다이어그램 (티센 폴리곤)</div>
```

- [ ] **Step 2: main.js import 추가**

`src/main.js`의 26행:

```js
import { bufferPanel } from './ui/panels/BufferPanel.js';
```

바로 아래에 추가:

```js
import { voronoiPanel } from './ui/panels/VoronoiPanel.js';
```

- [ ] **Step 3: 액션 case 추가**

`src/main.js`의 `case 'analysis-buffer'` 블록(739-747행) 바로 아래,
`case 'analysis-spatial-ops'` 앞에 추가한다.

패널이 자체 레이어 드롭다운을 갖고 있으므로 버퍼처럼 사전 선택을 요구하지 않는다.

```js
    case 'analysis-voronoi':
      voronoiPanel.show();
      break;
```

- [ ] **Step 4: 빌드 확인**

Run: `npm run build`

Expected: 성공

- [ ] **Step 5: 커밋**

```bash
git add src/ui/layout/AppLayout.js src/main.js
git commit -m "feat: 벡터 분석 메뉴에 보로노이 다이어그램 추가"
```

---

### Task 10: 수동 검증

단위 테스트는 순수 헬퍼만 덮는다. 나머지는 실제 앱을 몰아 확인한다.

**Files:** 없음 (검증만)

- [ ] **Step 1: 개발 서버 실행**

Run: `npm run dev`

브라우저에서 `http://localhost:3000` 이 자동으로 열린다.

- [ ] **Step 2: 사각형 모드**

내장 데이터나 좌표 데이터 가져오기로 포인트 레이어를 하나 만든다.
메뉴 → 벡터 분석 → 보로노이 다이어그램 (티센 폴리곤) → 생성.

확인:
- 셀이 지도에 그려진다
- 속성 테이블에 **원본 필드 + `area_km2`** 가 있다
- 셀 개수 = 포인트 개수

- [ ] **Step 3: 면적 교차 검증 (좌표계 결정의 핵심 검사)**

셀 하나를 골라 메뉴 → 측정 → 면적 측정으로 테두리를 따라 재고,
속성 테이블의 `area_km2` 값과 비교한다.

Expected: 손으로 그린 오차 범위 안에서 일치.
크게 어긋나면 EPSG:3857 계산 결정(`VoronoiTool.js` 상단 주석 참조)을 재검토해야 한다.

- [ ] **Step 4: 경계 클립**

내장 시도 경계 GeoJSON을 불러온 뒤 보로노이 패널의 경계 레이어로 지정하고 생성.

확인:
- 셀이 경계 밖으로 나가지 않는다
- 해안선을 따라 잘린다
- 경계 밖 포인트가 있었다면 "경계 밖 셀 N개 제외" 가 뜬다

- [ ] **Step 5: 티센 → 단계구분도 (용도 2의 성공 기준)**

생성된 보로노이 레이어를 선택하고 메뉴 → 벡터 분석 → 단계구분도.
원본 포인트의 수치 필드(예: 강수량)로 단계구분도가 그려지는지 확인한다.

- [ ] **Step 6: 중복 좌표**

같은 좌표를 두 번 넣은 CSV/좌표 데이터로 포인트 레이어를 만들고 생성.

Expected: 크래시 없이 "중복 좌표 1개 제외" 가 결과 알림에 뜬다.
`TypeError: Cannot read properties of null` 이 뜨면 `dedupeSeeds`가 안 걸린 것이다.

- [ ] **Step 7: 점 2개**

포인트가 2개뿐인 레이어로 생성.

Expected: 수직이등분선 하나로 나뉜 셀 2개.

- [ ] **Step 8: `area_km2` 충돌**

`area_km2` 컬럼이 있는 CSV로 포인트 레이어를 만들고 생성.

Expected: 원본 `area_km2`가 보존되고 새 필드는 `area_km2_1`로 붙는다.

- [ ] **Step 9: 왕복**

- 프로젝트를 `.egis`로 저장 → 새로 열기 → 보로노이 레이어가 복원되고 속성이 남아 있는지
- 레이어 내보내기 → SHP → `area_km2` 필드가 유지되는지

- [ ] **Step 10: 결과 기록**

검증 중 발견한 문제를 이 파일 아래 "검증 결과" 절에 적는다.
전부 통과하면 통과했다고 적는다.

---

## 검증 결과

(Task 10 수행 후 기록)
