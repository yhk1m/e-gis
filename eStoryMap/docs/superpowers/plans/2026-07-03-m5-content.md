# e-GIStory M5: 콘텐츠 (ContentEditor + 마크다운) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 우측 CONTENT 패널에서 현재 페이지의 제목(heading)/본문(body, 마크다운)/캡션(caption)을 편집하고, 본문 아래 살균된 마크다운 미리보기를 실시간으로 보여준다.

**Architecture:** 콘텐츠는 M3 모델의 `page.content`(heading/body/caption — 이미 자리 확보됨)에 저장한다. `StoryDoc.setPageContent`(순수 부분 패치, 알 수 없는 필드 무시)로 변이하고, `shared/markdown.js`의 `renderMarkdown`(marked 렌더 → DOMPurify 살균, M9/M10 뷰어와 공유)이 HTML을 만든다. **포커스 유지가 핵심 설계**: ContentEditor의 필드는 팩토리에서 1회 생성(재렌더 없음), 타이핑은 `onChange`로 문서만 갱신하고 미리보기는 컴포넌트가 자체 갱신 — `refresh()`를 타지 않는다. 페이지 전환 시에만 `render(page)`로 필드를 다시 채운다.

**Tech Stack:** 기존 스택 + **marked + dompurify**(신규 의존성 — 상위 스펙 §0 확정). Vitest/jsdom에서 DOMPurify는 window 자동 감지로 동작.

---

## 이 계획의 범위 (중요)

상위 스펙 `eStoryMap-PLAN.md` §7 **M5만**: "콘텐츠: ContentEditor(heading/body/caption). body는 마크다운(marked 렌더 + DOMPurify 살균)." 다음은 **의도적으로 연기(YAGNI)**:

- **서식 툴바**(마크다운 문법 없이 굵게/목록 버튼) — v2(§0·§8b 명시).
- 뷰어에서의 콘텐츠 표시(4:3 오버레이 카드, A4 본문) — M9/M10. `renderMarkdown`은 그때 재사용.
- 페이지 제목(title) 이름 변경 UI — 백로그(콘텐츠 heading과 별개인 PageList 라벨).
- 이미지 삽입/첨부 — 백로그.

M5 완료 기준: "**페이지마다 다른 제목/본문/캡션을 입력하면 페이지 전환 시 그 페이지의 내용이 채워지고, 본문에 `# 제목`·`**굵게**`·목록을 쓰면 미리보기에 렌더되며, `<script>`는 살균된다. 타이핑 중 포커스가 유지된다.**"

## 현재 코드 기준 확정 사실 (2026-07-03, 122 tests green, HEAD db477bc)

1. `page.content = { heading: '', body: '', caption: '' }` — M3 `makePage`에서 이미 생성됨(스펙 §2). `addPage`는 content를 복사하지 않음(빈 값으로 시작 — §4 "레이어/카메라만 복제"와 일관).
2. `src/main.js` — `refresh()` 단일 갱신 지점(applyPageVisibility + sourcePanel.render + pageList.render), `let currentPageId`, StoryDoc import 블록, `#workspace` 안에 `#sidebar`(좌) + `#map-stage`(중앙). **우측 패널은 아직 없음.**
3. `index.html` — `#workspace` = `<aside id="sidebar">…</aside>` + `<div id="map-stage">…</div>`. §4 목업의 3패널(SOURCE|CANVAS|CONTENT) 중 CONTENT를 이번에 추가.
4. DOM 컴포넌트 관례: `src/editor/`의 factory 패턴(`createXxx(container, handlers)` → `{render}`), jsdom에서 `dispatchEvent(new Event('input'))`로 리스너 검증 가능(SourcePanel/PageList 선례).
5. StoryDoc 변이 관례: `getPage` null 가드 → 조용한 no-op, `touch(doc)` 호출.
6. marked: `marked.parse(text)`는 동기 문자열 반환(기본). DOMPurify: `DOMPurify.sanitize(html)` — 렌더러(브라우저)와 vitest(jsdom) 모두 동작.
7. ⚠️ vite.config.js `renderer` 플러그인 금지(기존 함정). marked/dompurify는 순수 브라우저 호환 패키지라 표준 옵티마이저로 문제없음(geotiff류 Node 엔트리 이슈 없음).
8. `npm run dev`는 사람 스모크 전용(서브에이전트 실행 금지).

## File Structure

앱 루트: `C:/Users/김용현/Desktop/vibecoding/eGIS/eStoryMap/` (이하 상대경로).

| 파일 | 책임 |
|---|---|
| Modify: `src/core/StoryDoc.js` | `setPageContent(doc, pageId, patch)` — 순수 부분 패치(허용 필드만). |
| Create: `src/shared/markdown.js` | `renderMarkdown(text)` — marked 렌더 + DOMPurify 살균. M9/M10과 공유. |
| Create: `src/editor/ContentEditor.js` | 우측 CONTENT 패널 DOM 컴포넌트(필드 1회 생성, 미리보기 자체 갱신). |
| Modify: `src/main.js`, `index.html`, `src/style.css` | 우측 패널 배선(onChange→setPageContent, refresh→render). |
| Modify: `package.json` | marked, dompurify 추가(npm install). |
| Test: `src/core/StoryDoc.test.js`(추가), `src/shared/markdown.test.js`(신규), `src/editor/ContentEditor.test.js`(신규) | 단위 테스트. |

현재 테스트 기준선: **122개**. 완료 시 **137개**(신규 15).

---

## Task 1: StoryDoc.setPageContent (순수, TDD)

**Files:**
- Test: `src/core/StoryDoc.test.js` (추가)
- Modify: `src/core/StoryDoc.js`

- [ ] **Step 1: 실패하는 테스트 추가** — `src/core/StoryDoc.test.js`의 import에 `setPageContent` 추가 후 파일 끝에:

```js
describe('setPageContent', () => {
  it('부분 패치는 해당 필드만 갱신하고 나머지는 보존한다', () => {
    const doc = createStoryDoc();
    setPageContent(doc, 'page_1', { heading: '부산의 인구' });
    setPageContent(doc, 'page_1', { body: '# 개요' });
    expect(getPage(doc, 'page_1').content).toEqual({
      heading: '부산의 인구', body: '# 개요', caption: '',
    });
  });

  it('여러 필드를 한 번에 패치할 수 있다', () => {
    const doc = createStoryDoc();
    setPageContent(doc, 'page_1', { heading: '제목', body: '본문', caption: '캡션' });
    expect(getPage(doc, 'page_1').content).toEqual({
      heading: '제목', body: '본문', caption: '캡션',
    });
  });

  it('없는 페이지/빈 patch는 no-op', () => {
    const doc = createStoryDoc();
    setPageContent(doc, 'page_999', { heading: 'x' });
    setPageContent(doc, 'page_1', null);
    expect(getPage(doc, 'page_1').content.heading).toBe('');
  });

  it('알 수 없는 필드와 문자열이 아닌 값은 무시한다(.esm 오염 방지)', () => {
    const doc = createStoryDoc();
    setPageContent(doc, 'page_1', { heading: '유지', evil: 'x', body: 123 });
    const content = getPage(doc, 'page_1').content;
    expect(content).toEqual({ heading: '유지', body: '', caption: '' });
    expect('evil' in content).toBe(false);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd "C:/Users/김용현/Desktop/vibecoding/eGIS/eStoryMap" && npx vitest run src/core/StoryDoc.test.js`
Expected: 새 4개 FAIL(export 없음), 기존 22개 PASS.

- [ ] **Step 3: 구현** — `src/core/StoryDoc.js`에 추가(`setPageCamera` 아래):

```js
const CONTENT_FIELDS = ['heading', 'body', 'caption'];

/**
 * 페이지 콘텐츠 부분 패치(M5). 허용 필드(heading/body/caption)의 문자열 값만
 * 반영하고 그 외는 무시한다(.esm에 임의 키가 스며드는 것 방지).
 */
export function setPageContent(doc, pageId, patch) {
  const page = getPage(doc, pageId);
  if (!page || !patch) return;
  for (const field of CONTENT_FIELDS) {
    if (typeof patch[field] === 'string') page.content[field] = patch[field];
  }
  touch(doc);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/core/StoryDoc.test.js` — Expected: PASS (26 tests). 전체 126.

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/김용현/Desktop/vibecoding/eGIS" && git add eStoryMap/src/core/StoryDoc.js eStoryMap/src/core/StoryDoc.test.js && git commit -m "feat(eStoryMap): M5 setPageContent — 페이지 콘텐츠 부분 패치(순수)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 2: shared/markdown.js — 마크다운 렌더+살균 (TDD)

**Files:**
- Modify: `package.json` (npm install)
- Test: `src/shared/markdown.test.js`
- Create: `src/shared/markdown.js`

- [ ] **Step 1: 의존성 설치**

Run: `cd "C:/Users/김용현/Desktop/vibecoding/eGIS/eStoryMap" && npm install marked dompurify`
Expected: package.json dependencies에 `marked`, `dompurify` 추가, 에러 없음.

- [ ] **Step 2: 실패하는 테스트 작성** — `src/shared/markdown.test.js`

```js
// © 2026 김용현
import { describe, it, expect } from 'vitest';
import { renderMarkdown } from './markdown.js';

describe('renderMarkdown', () => {
  it('헤딩을 렌더한다', () => {
    const html = renderMarkdown('# 부산의 인구');
    expect(html).toContain('<h1');
    expect(html).toContain('부산의 인구');
  });

  it('굵게/목록 등 기본 문법을 렌더한다', () => {
    expect(renderMarkdown('**굵게**')).toContain('<strong>굵게</strong>');
    const list = renderMarkdown('- 하나\n- 둘');
    expect(list).toContain('<ul>');
    expect(list.match(/<li>/g)).toHaveLength(2);
  });

  it('script 태그를 살균한다', () => {
    const html = renderMarkdown('주의 <script>alert(1)</script> 문구');
    expect(html).not.toContain('<script');
    expect(html).toContain('주의');
    expect(html).toContain('문구');
  });

  it('이벤트 핸들러 속성을 살균한다', () => {
    const html = renderMarkdown('<img src="x" onerror="alert(1)">');
    expect(html).not.toContain('onerror');
  });

  it('빈 입력은 빈 문자열', () => {
    expect(renderMarkdown('')).toBe('');
    expect(renderMarkdown(null)).toBe('');
    expect(renderMarkdown(undefined)).toBe('');
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npx vitest run src/shared/markdown.test.js` — Expected: FAIL — `markdown.js` 없음.

- [ ] **Step 4: 최소 구현** — `src/shared/markdown.js`

```js
// © 2026 김용현
// eStoryMap/src/shared/markdown.js
// 본문 마크다운 → 살균된 HTML(상위 스펙 §0: marked 렌더 + DOMPurify 살균).
// 에디터 미리보기(M5)와 뷰어(M9 프레젠테이션 / M10 보고서)가 공유한다.
import { marked } from 'marked';
import DOMPurify from 'dompurify';

/** 마크다운 → 안전한 HTML 문자열. 빈 입력은 빈 문자열. */
export function renderMarkdown(text) {
  if (!text) return '';
  const html = marked.parse(text);
  return DOMPurify.sanitize(html);
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npx vitest run src/shared/markdown.test.js` — Expected: PASS (5 tests). 전체 131.

- [ ] **Step 6: 커밋**

```bash
cd "C:/Users/김용현/Desktop/vibecoding/eGIS" && git add eStoryMap/package.json eStoryMap/package-lock.json eStoryMap/src/shared/markdown.js eStoryMap/src/shared/markdown.test.js && git commit -m "feat(eStoryMap): M5 renderMarkdown — marked 렌더+DOMPurify 살균(뷰어 공유용)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 3: ContentEditor — 우측 콘텐츠 패널 (DOM, TDD)

**Files:**
- Test: `src/editor/ContentEditor.test.js`
- Create: `src/editor/ContentEditor.js`

- [ ] **Step 1: 실패하는 테스트 작성** — `src/editor/ContentEditor.test.js`

```js
// © 2026 김용현
import { describe, it, expect, vi } from 'vitest';
import { createContentEditor } from './ContentEditor.js';

function pageWith(content) {
  return { content: { heading: '', body: '', caption: '', ...content } };
}

function make(onChange = vi.fn()) {
  const el = document.createElement('div');
  const editor = createContentEditor(el, { onChange });
  return { el, editor, onChange };
}

describe('ContentEditor', () => {
  it('render가 페이지 콘텐츠로 필드와 미리보기를 채운다', () => {
    const { el, editor } = make();
    editor.render(pageWith({ heading: '제목A', body: '# 본문A', caption: '캡션A' }));
    expect(el.querySelector('#content-heading').value).toBe('제목A');
    expect(el.querySelector('#content-body').value).toBe('# 본문A');
    expect(el.querySelector('#content-caption').value).toBe('캡션A');
    expect(el.querySelector('#content-preview').innerHTML).toContain('<h1');
  });

  it('heading 입력 → onChange("heading", 값)', () => {
    const { el, onChange } = make();
    const input = el.querySelector('#content-heading');
    input.value = '새 제목';
    input.dispatchEvent(new Event('input'));
    expect(onChange).toHaveBeenCalledWith('heading', '새 제목');
  });

  it('body 입력 → onChange("body", 값) + 미리보기 즉시 갱신', () => {
    const { el, onChange } = make();
    const body = el.querySelector('#content-body');
    body.value = '# 실시간';
    body.dispatchEvent(new Event('input'));
    expect(onChange).toHaveBeenCalledWith('body', '# 실시간');
    expect(el.querySelector('#content-preview').innerHTML).toContain('<h1');
    expect(el.querySelector('#content-preview').textContent).toContain('실시간');
  });

  it('caption 입력 → onChange("caption", 값)', () => {
    const { el, onChange } = make();
    const input = el.querySelector('#content-caption');
    input.value = '출처: 통계청';
    input.dispatchEvent(new Event('input'));
    expect(onChange).toHaveBeenCalledWith('caption', '출처: 통계청');
  });

  it('미리보기는 살균된다(script/onerror 제거)', () => {
    const { el } = make();
    const body = el.querySelector('#content-body');
    body.value = '<script>alert(1)</script><img src="x" onerror="alert(1)">안전';
    body.dispatchEvent(new Event('input'));
    const html = el.querySelector('#content-preview').innerHTML;
    expect(html).not.toContain('<script');
    expect(html).not.toContain('onerror');
    expect(el.querySelector('#content-preview').textContent).toContain('안전');
  });

  it('다른 페이지로 render하면 필드가 그 페이지 값으로 바뀐다', () => {
    const { el, editor } = make();
    editor.render(pageWith({ heading: 'A' }));
    expect(el.querySelector('#content-heading').value).toBe('A');
    editor.render(pageWith({ heading: 'B', body: '내용B' }));
    expect(el.querySelector('#content-heading').value).toBe('B');
    expect(el.querySelector('#content-body').value).toBe('내용B');
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/editor/ContentEditor.test.js` — Expected: FAIL — 모듈 없음.

- [ ] **Step 3: 최소 구현** — `src/editor/ContentEditor.js`

```js
// © 2026 김용현
// eStoryMap/src/editor/ContentEditor.js
// 우측 CONTENT 패널: 현재 페이지의 heading/body(마크다운)/caption 편집(상위 스펙 §4).
// 필드는 팩토리에서 1회 생성 — 입력 중 재렌더가 없어야 포커스가 유지된다.
// 타이핑은 onChange로 문서에만 반영하고(전체 refresh 금지), 미리보기는 자체 갱신.
// MVP는 순수 마크다운 입력(서식 툴바는 v2 — 스펙 §0).
import { renderMarkdown } from '../shared/markdown.js';

/**
 * @param {HTMLElement} container
 * @param {{onChange(field:'heading'|'body'|'caption', value:string):void}} handlers
 */
export function createContentEditor(container, { onChange }) {
  container.innerHTML = '';

  const heading = document.createElement('input');
  heading.type = 'text';
  heading.id = 'content-heading';
  heading.placeholder = '페이지 제목';

  const body = document.createElement('textarea');
  body.id = 'content-body';
  body.placeholder = '본문 (마크다운: # 제목, **굵게**, - 목록)';
  body.rows = 10;

  const preview = document.createElement('div');
  preview.id = 'content-preview';
  preview.className = 'md-preview';

  const caption = document.createElement('input');
  caption.type = 'text';
  caption.id = 'content-caption';
  caption.placeholder = '지도 하단 캡션';

  heading.addEventListener('input', () => onChange('heading', heading.value));
  body.addEventListener('input', () => {
    preview.innerHTML = renderMarkdown(body.value); // 살균된 HTML만 삽입
    onChange('body', body.value);
  });
  caption.addEventListener('input', () => onChange('caption', caption.value));

  for (const [label, el] of [['제목', heading], ['본문', body], ['미리보기', preview], ['캡션', caption]]) {
    const wrap = document.createElement('div');
    wrap.className = 'content-field';
    const lab = document.createElement('div');
    lab.className = 'content-label';
    lab.textContent = label;
    wrap.appendChild(lab);
    wrap.appendChild(el);
    container.appendChild(wrap);
  }

  /** 페이지 전환 시 현재 페이지 콘텐츠로 필드를 채운다. */
  function render(page) {
    heading.value = page.content.heading;
    body.value = page.content.body;
    caption.value = page.content.caption;
    preview.innerHTML = renderMarkdown(page.content.body);
  }

  return { render };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/editor/ContentEditor.test.js` — Expected: PASS (6 tests). 전체 137.

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/김용현/Desktop/vibecoding/eGIS" && git add eStoryMap/src/editor/ContentEditor.js eStoryMap/src/editor/ContentEditor.test.js && git commit -m "feat(eStoryMap): M5 ContentEditor — 콘텐츠 편집 패널+살균 미리보기(DOM)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Task 4: 배선 — 우측 패널 + 콘텐츠 흐름 (M5 완료)

**Files:**
- Modify: `index.html`, `src/style.css`, `src/main.js`

- [ ] **Step 1: `index.html`** — `#workspace` 안, `#map-stage` 닫는 `</div>` **다음**에 우측 패널 추가:

```html
        <aside id="content-sidebar">
          <section class="panel">
            <h2 class="panel-title">콘텐츠</h2>
            <div id="content-panel"></div>
          </section>
        </aside>
```

- [ ] **Step 2: `src/style.css` 끝에 추가**

```css
/* --- M5 콘텐츠 패널 --- */
#content-sidebar {
  width: 260px; flex: none; overflow-y: auto;
  background: #0f172a; color: #e5e7eb; border-left: 1px solid #1e293b;
}
.content-field { margin-bottom: 12px; }
.content-label { font-size: 12px; color: #94a3b8; margin-bottom: 4px; }
#content-panel input, #content-panel textarea {
  width: 100%; padding: 6px 8px; border: 1px solid #334155; border-radius: 6px;
  background: #1e293b; color: #e5e7eb; font-size: 13px; font-family: inherit; resize: vertical;
}
.md-preview {
  border: 1px dashed #334155; border-radius: 6px; padding: 8px;
  font-size: 13px; min-height: 40px; overflow-wrap: break-word;
}
.md-preview h1, .md-preview h2, .md-preview h3 { margin: 4px 0; }
.md-preview p { margin: 4px 0; }
.md-preview ul, .md-preview ol { margin: 4px 0 4px 18px; padding: 0; }
```

- [ ] **Step 3: `src/main.js` 배선** — 세 군데:

① StoryDoc import에 `setPageContent` 추가, ContentEditor import 추가:

```js
import {
  createStoryDoc, addSource, addPage, removePage, getPage, setLayerVisible, nextSourceId,
  setPageCamera, setPageContent,
} from './core/StoryDoc.js';
import { createContentEditor } from './editor/ContentEditor.js';
```

② `pageList` 팩토리 아래에 추가:

```js
const contentEditor = createContentEditor(document.getElementById('content-panel'), {
  onChange(field, value) {
    // 전체 refresh 없음 — 타이핑 중 포커스 유지(콘텐츠는 지도/패널에 영향 없음)
    setPageContent(doc, currentPageId, { [field]: value });
  },
});
```

③ `refresh()`에 한 줄 추가(마지막 줄로):

```js
function refresh() {
  const page = getPage(doc, currentPageId) || doc.pages[0]; // 방어: 선택이 어긋나도 자가 복구
  applyPageVisibility(page, registry);
  sourcePanel.render(doc, page, registry);
  pageList.render(doc, currentPageId);
  contentEditor.render(page);
}
```

- [ ] **Step 4: 전체 테스트 + 빌드 확인**

Run: `cd "C:/Users/김용현/Desktop/vibecoding/eGIS/eStoryMap" && npm test` — Expected: PASS (**137 tests**).
Run: `rm -rf dist && npx vite build 2>&1 | tail -3` — Expected: 에러 없이 완료(dist 스테이징 금지 — 기존 dist가 있으면 exit 127 함정 주의).

- [ ] **Step 5: 수동 스모크 (M5 완료 판정 — 사용자 육안 확인)**

Run: `npm run dev`
Expected:
1. 우측에 "콘텐츠" 패널(제목/본문/미리보기/캡션).
2. 페이지 1에서 제목 "부산", 본문 `# 개요\n**인구**가 많다\n- 첫째\n- 둘째` 입력 → 미리보기에 헤딩·굵게·목록 렌더. **타이핑 중 포커스 유지**(한 글자마다 끊기지 않음).
3. `+ 페이지` → 콘텐츠 필드가 빈 값(콘텐츠는 복제 안 됨 — 레이어/카메라만 복제).
4. 페이지 2에 다른 내용 입력 → 페이지 1↔2 전환 시 각자 내용으로 채워짐.
5. 본문에 `<script>alert(1)</script>` 입력 → 미리보기에서 무해(알림 안 뜸).
6. 체크박스/캡처 등 기존 기능 회귀 없음.

- [ ] **Step 6: 커밋**

```bash
cd "C:/Users/김용현/Desktop/vibecoding/eGIS" && git add eStoryMap/index.html eStoryMap/src/style.css eStoryMap/src/main.js && git commit -m "feat(eStoryMap): M5 배선 — 우측 콘텐츠 패널(3패널 완성)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## Self-Review

**1. 스펙 커버리지 (M5 범위):**
- §7 M5 "ContentEditor(heading/body/caption)" → Task 3 + Task 4(§4의 3패널 CONTENT 완성). ✓
- §7 M5 "body는 마크다운(marked 렌더 + DOMPurify 살균)" → Task 2(renderMarkdown, 살균 테스트 포함), 미리보기로 즉시 검증. ✓
- §0 "마크다운 저장, MVP는 순수 마크다운 입력, 서식 툴바 v2" → 저장은 원문(마크다운 텍스트) 그대로 `page.content.body`에(Task 1), 렌더는 표시 시점에만. 툴바 없음. ✓
- §2 content 모델 — M3에서 자리 확보, Task 1이 변이 추가. ✓

**2. 플레이스홀더 스캔:** 없음 — 모든 단계에 실제 코드. ✓

**3. 타입/이름 일관성:** `setPageContent(doc, pageId, patch)`(StoryDoc) · `renderMarkdown(text)`(shared) · `createContentEditor(container, {onChange}).render(page)`(editor) — Task 4 main.js가 동일 시그니처로 호출. onChange 필드명('heading'|'body'|'caption')이 CONTENT_FIELDS·content 모델 키와 일치. `#content-panel`(index.html) ↔ `getElementById('content-panel')`. refresh()의 기존 4줄 유지 + 1줄 추가(방어 가드 포함 현행 코드 그대로). ✓

**미해결(구현 중 실측):**
- marked 최신 버전의 `marked.parse` 동기 반환 확인(기본 동작 — 비동기 확장 미사용이라 통과 기대). 만약 Promise가 반환되면 BLOCKED로 보고(플랜 수정 필요).
- DOMPurify가 vitest/jsdom에서 window 자동 감지로 동작하는지 → Task 2 첫 실행에서 확인(공식 지원이라 통과 기대).
- 우측 패널 추가 후 지도 폭 축소 — OL ResizeObserver가 자동 처리(M3 사이드바 때와 동일). 스모크에서 확인.
- 본문이 매우 길 때 미리보기 성능(입력마다 재렌더) — M5 분량(짧은 슬라이드 본문)에선 무시 가능. 백로그: debounce.
